import React from "react";

import classes from "./SavedPasswords.module.css";

const SavedPasswords = (props) => {
  return (
    <div className={classes["saved-passwords"]}>
      <h2>SavedPasswords</h2>
    </div>
  );
};

export default SavedPasswords;
